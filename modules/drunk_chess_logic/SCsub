# SCsub

Import('env')

env_dclogic = env.Clone()
env_dclogic.add_source_files(env.modules_sources, "*.cpp") # Add all cpp files to the build
#env_dclogic.add_source_files(env.modules_sources, "return2/*.cpp") # Add all cpp files to the build
